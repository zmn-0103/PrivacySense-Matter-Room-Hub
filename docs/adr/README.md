# Architecture Decision Records

此目录保存已作出且需要长期追溯的架构决策。一个 ADR 使用 `ADR-<编号>-<简短英文标题>.md` 命名，并包含：状态、背景、决策、备选方案、影响、验证方式、相关任务和批准人/日期。

需要 ADR 的典型变化包括：任务/队列模型、公共接口、Matter 数据模型、存储格式、资源预算、OTA 与回滚策略。硬件事实必须引用 `docs/approved_sources/` 中的已批准资料。
