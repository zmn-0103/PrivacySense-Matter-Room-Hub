# AGENTS.md

## 1. 项目目标

本项目是 `PrivacySense Matter Room Hub`：基于 ESP32-C6 的无摄像头房间状态中枢。首版使用毫米波雷达和环境传感器进行本地状态判断，通过 BLE 完成首次配网，通过 Matter over Wi-Fi 接入智能家居自动化。

## 2. AI 角色分工

### ChatGPT / Codex：Reviewer AI

ChatGPT / Codex 在本项目中只承担：

- 复查 Builder AI 提交的代码。
- 检查 bug、竞态、边界条件、资源泄漏、任务阻塞和协议状态错误。
- 检查 FreeRTOS 任务、队列、事件组、互斥锁和中断使用。
- 检查 Wi-Fi、BLE commissioning、Matter 状态同步和传感器异常处理。
- 检查功耗、看门狗、断网恢复、配置持久化和 OTA 风险。
- 根据现有代码提出测试用例、复现步骤和修复建议。

Reviewer AI 默认不得：

- 编写、修改或重构 `firmware/`、`tests/`、`tools/` 中的业务代码。
- 代替 Builder AI 实现新功能。
- 通过注释、关闭检查或降低编译警告掩盖问题。
- 擅自修改硬件连接、Matter 数据模型或公共接口。
- 擅自编译、烧录、连接硬件或修改环境配置。

如果用户明确要求 Reviewer AI 执行文件修改操作，Reviewer AI 可以执行。除此之外，如果用户要求 Reviewer AI 直接写固件代码，应说明本角色约束，并返回设计、伪代码、接口建议或 review 意见，由 Builder AI 完成实现。项目文档只有在用户明确要求时才能由 Reviewer AI 创建或修改。

### Other AI：Builder AI

其他 AI 负责编写和修改固件、测试代码及辅助脚本，执行编译、静态检查、单元测试和硬件验证，并提供文件清单、验证命令、结果和已知风险。Builder AI 不得修改本文件定义的 Reviewer AI 角色边界。

## 3. 首版技术边界

- 主控固定为 ESP32-C6 开发板，除非用户明确批准更换。
- 首版采用 Matter over Wi-Fi；不把 Thread 与 Wi-Fi 同时运行作为首版目标。
- BLE 用于 commissioning 或维护，不通过扫描手机 MAC 地址判断用户身份。
- 不使用摄像头，不上传原始雷达数据，不保存可识别个人身份的数据。
- 只使用低压 USB 供电和低压负载，不直接连接 110/220 V 市电。
- 房间状态只能用于家居自动化，不得宣传为医疗、生命监测或专业安防功能。
- 传感器异常、网络断开、Matter 会话失败时，设备必须保留本地可用性。

## 4. 固件工程要求

- GPIO、UART、I2C 地址和供电电压必须写入连接表或配置文件，不得散落硬编码。
- ISR 必须短小，不得执行阻塞操作、复杂协议解析或动态内存分配。
- 传感器数据通过任务、队列或事件传递，禁止无保护访问共享状态。
- 所有任务必须有明确周期、超时和栈空间依据，并记录 `uxTaskGetStackHighWaterMark()`。
- 网络、BLE、Matter 和传感器错误必须有超时、重试上限和退避策略。
- 动态内存必须有上限、失败处理和生命周期说明；优先使用静态或预分配对象。
- 必须启用并验证 watchdog、异常日志和关键状态日志。
- 不在日志、代码或提交中写入 Wi-Fi 密码、Matter setup payload、私钥或其他敏感信息。
- OTA 必须说明镜像校验、版本回退策略和断电行为，不得仅凭下载成功宣称可靠。

## 5. 验证要求

Builder AI 每次提交至少说明：修改目标和文件、编译/静态检查/测试命令、断网和传感器异常结果、Flash/RAM/任务栈/功耗数据或缺口、未解决问题。

硬件验证至少覆盖：首次 BLE 配网和重新配网、Wi-Fi 断开恢复、Matter 状态上报、控制器重启、毫米波误触发、持续有人/无人、传感器断线、断电重启、配置持久化和 watchdog 复位。

## 6. Git 与安全规则

- 不提交密钥、密码、私钥、真实 Matter setup payload 或私人网络信息。
- 不执行 `git reset --hard`、强制推送、批量删除或历史改写，除非用户明确授权。
- 不自动推送远程仓库，不自动创建 Pull Request。
- 提交信息遵循 `<type>(<scope>): <description>`，描述使用英文。
- 源代码改动必须由 Builder AI 提交给 Reviewer AI 复查。
